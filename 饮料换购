#include <iostream>
using namespace std;
int main()
{
    int n,yu=0,count;//初始数目，余数，总数
    cin >> n;
    count = n;
    while (n >= 3) {
    yu = n % 3;
    n = n / 3;
    count += n;
    n += yu;
    }
    cout << count;
    return 0;
}
