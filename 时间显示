#include <bits/stdc++.h>
using namespace std;
int h,m,s;
int main()
{
  long long x;
  cin>>x;
  h = (x/3600000)%24;
  m = (x/60000)%60;
  s = (x/1000)%60;
  cout<<setw(2)<<setfill('0')<<h<<':'<<setw(2)<<setfill('0')<<m<<':'<<setw(2)<<setfill('0')<<s<<endl;
  return 0;
}
