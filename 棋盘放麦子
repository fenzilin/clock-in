#include <bits/stdc++.h>
using namespace std;
unsigned long long res = 1,s;
int main()
{
  // 请在此输入您的代码
  for(int i = 0; i < 64; i++)
  {
     s += res;
     res *= 2;
     
  }
  printf("%llu",s);
  return 0;
}
